# https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: npm run lint
    - name: types
      run: npm run check-types
    - name: test
      run: npm test
